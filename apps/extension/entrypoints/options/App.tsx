export function App() {
  return (
    <main style={{ maxWidth: 640, margin: '40px auto', fontFamily: 'system-ui', padding: 16 }}>
      <h1 style={{ fontSize: 20 }}>Settings</h1>
      <p style={{ color: '#555' }}>
        AI provider configuration goes here (API keys are stored locally and masked; never
        exported, never in Trace/Diagnostics). Skeleton placeholder.
      </p>
    </main>
  )
}
